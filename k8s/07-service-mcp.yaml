apiVersion: v1
kind: Service
metadata:
  name: archon-mcp
  namespace: unlkd
  labels:
    app.kubernetes.io/name: archon-mcp
    app.kubernetes.io/instance: archon
    app.kubernetes.io/component: mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8051
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: archon-mcp
    app.kubernetes.io/instance: archon
