apiVersion: v1
kind: Service
metadata:
  name: archon-mcp-service
  labels:
    app.kubernetes.io/component: mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8051
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/component: mcp
