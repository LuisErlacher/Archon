name: tmp-codex-terminal-check
description: Temporary workflow to verify Codex can execute terminal commands.
provider: codex

nodes:
  - id: check-terminal
    prompt: |
      Run the terminal command `pwd`.
      Then run the terminal command `echo ARCHON_CODEX_TERMINAL_OK`.
      Return both outputs clearly.
